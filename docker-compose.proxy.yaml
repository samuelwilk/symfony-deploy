services:
  proxy:
    image: caddy:2
    container_name: symfony-deploy-proxy
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile
      - ./current:/srv
    working_dir: /srv
